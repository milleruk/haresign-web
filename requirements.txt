Django==5.1.6
gunicorn==23.0.0
whitenoise[brotli]==6.8.2

# Insights (Web-owned editorial content)
psycopg[binary]==3.2.3      # PostgreSQL driver (psycopg 3)
django-tinymce==4.1.0       # Jazzband; the same authoring model as the monolith
Pillow==11.1.0              # ImageField support for featured images
